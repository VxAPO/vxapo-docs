# VxAPO 产品意图与用户行为模型

> **本文件是 VxAPO 的产品定义**：产品是什么、为谁服务、边界在哪、约束从哪来。
> 它是规范的**上位**——所有规范修订（路线清单 roadmap、模块规范、changelog）
> 必须与本文件一致。本文件不是路线图，不是变更记录，而是产品本身的定义。

---

## 一、产品定位

**VxAPO 是一个 Windows 音频 APO（Audio Processing Object）DSP 引擎，配备设备级配置管理。**

- **内核**：以 DLL（vxapo.dll）形态注入 audiodg.exe，对每个音频端点提供实时 DSP 处理
  （均衡、增益、延迟、卷积、响度校正等），以 Filter 链 + 双链过渡 + 热重载为核心机制。
- **配置**：每个设备一份 `Documents\VxAPO\{GUID}\config.txt`，经解析构建 Filter 链。
- **管理**：CLI / 未来 UI 负责设备安装卸载、配置导入选择、预设管理、实时调整。

**产品信念**：

1. **音频是实时的**——RT 路径零分配、零锁、零 I/O 是铁律（R1-R4，`apo.rs 线程安全模型.md`）。
2. **配置是设备级的**——每个端点独立配置，互不干扰。
3. **改配置不应打断声音**——热重载 + 升余弦过渡，无爆音、无静音空隙。
4. **用户通过产品界面管理配置，而非直接操作文件**——见「用户行为模型」。

---

## 二、用户行为模型

> 核心意图：**用户能做的只有通过应用/CLI 管理配置**。
> 直接操作 `Documents\VxAPO\{GUID}` 文件夹不是受支持的使用方式。

### 正常路径（受支持）

| 场景 | 用户动作 | 系统行为 |
|------|---------|---------|
| ① 安装设备 + 应用配置 | `vxapo-cli install -d <device>` → 导入/选择 config.txt | 写 FxProperties 绑定 + 写 config.txt → 下次音频引擎加载时生效 |
| ② UI 实时调整 | UI 页面选择设备 → 调整参数 | UI 写 config.txt → watcher 热重载 → 双链过渡 → 实时生效（无爆音） |
| ③ 切换预设 | `preset apply <name>` / UI 选择预设 | 覆盖 config.txt → 热重载生效 |
| ④ 解锁/重锁周期 | 引擎 UnlockForProcess / LockForProcess | Unlock 停 watcher + 清基线；重新 Lock 重读 config 建新基线 |

### 边缘情况（不支持，但系统需优雅降级）

| 场景 | 系统行为 |
|------|---------|
| ⑤ 用户**手动**修改文件夹内 config.txt | watcher 确实会触发 hot_reload——但这是**边缘情况**：
  日志记录（log::warn/debug）——**配置实质变化时保留旧链并过渡**（7.1.18），
  **内容未变时幂等跳过**（spec 指纹比对，6.1）；
  且下轮 UI/CLI 调整会覆盖此文件的任何手动修改 |
| ⑥ config 损坏 / 语法错误 | 解析失败 → **保留当前生效链**（EQ 不消失），日志告警（7.1.18） |
| ⑦ config 文件过大（>128KB） | 拒绝读取，保留旧链，日志告警（控制线程 IO 安全上限，逐文件判定） |
| ⑧ Documents 不可用 / 无设备 GUID | 二级兜底（`_default` / `C:\ProgramData\VxAPO\config.txt`），不阻断初始化（7.1.8） |

> **原则**：边缘情况可以发生，但必须**无副作用地降级**——不崩溃、不出爆音、
> 不静音、不残留错误状态，并留下日志供诊断。

---

## 三、驱动层 vs 应用层边界

| 层 | 职责 | 归属 |
|----|------|------|
| **驱动层（DLL / vxapo-driver）** | APO COM 生命周期、格式协商、Filter 链解析与构建、watcher 热重载、双链过渡、RT 安全、设备 config 路径解析 | 本规范仓库（`模块引用规范（无详细模块版）.md` + 子规范） |
| **应用层（CLI / 未来 UI / App）** | 设备枚举与安装卸载、config 导入选择、预设管理、实时调整写入 config.txt | 规范仓库之外的新 crate（P1-2 声明边界：只读安装层 API，不触碰 pipeline/RT） |

**边界规则**：

- 驱动层**不做**预设管理、不做配置可视化、不主动写 config（仅兜底写默认 passthrough）。
- 应用层**不触碰** RT / pipeline / Chain——只通过「写 config.txt」间接驱动内核。
- 两者的接口是 **config.txt 文件 + 注册表 FxProperties**——应用层写文件，驱动层监听并生效。

---

## 四、设计与治理地位

```
intent.md（产品意图——为谁/为何做）
    ↓ 约束
roadmap.md（路线清单——要做什么）
    ↓ 约束
规范文档（模块引用规范 + 子规范——怎么做）
    ↓ 约束
实现（执行端 agent）
```

**治理规则**：

1. **规范修订须与 intent.md 一致**——若规范行为与「用户行为模型」「边界」冲突，以 intent.md 为上位修正规范。
2. **roadmap 条目须能追溯至 intent.md 的产品意图**——无法映射到用户可感知价值的功能不登记。
3. **changelog 记录规范变化**——按 `.clinerules/changelog-rule.md`。
4. **本文件变更本身属重大决策**——编辑需经用户确认，并同步评估三文件联动影响。

---

## 五、产品约束来源

| 约束 | 来源 |
|------|------|
| RT 零分配/零锁/零 I/O（R1-R4） | `apo.rs 线程安全模型.md` |
| 热重载保留旧链 / 升级失败不打断声音 | EAPO 实战 + 用户决策（v7.8/v7.9） |
| watcher 生命周期随锁定周期 | 用户决策（v7.9）：unlocked 期间无音频流 |
| 配置指纹（filter_spec）比对 | 用户决策（v7.9）：直接解析 + spec 比对，而非文件内容哈希 |
| 128KB 逐文件上限 | 用户决策（v7.9）：控制线程 IO 安全上限 |
| 用户不直接操作文件 | 本文件「用户行为模型」 |