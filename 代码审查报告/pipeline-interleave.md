# 代码审查报告：pipeline/interleave.rs

- 审查日期：2026-08-11
- 被审文件：`D:\APO_Project\VxAPO\vxapo-driver\src\pipeline\interleave.rs`
- 审查模块：`pipeline/interleave`（交织/去交织搬运）
- 参照规范：`pipeline 模块规范.md` 4.4、`模块引用规范（无详细模块版）.md` 第十一节
- 总体评价：✅ 通过

---

## 审查摘要

- RT 零分配路径（`deinterleave_into`/`interleave_from`/`interleave_from_guarded`）纯索引搬运；分配版本明确标注非 RT。`interleave_from_guarded` 的 NaN/inf→0 链级防线有价值。

---

## 可读性问题

| 行号 | 问题描述 | 建议 |
|------|----------|------|
| 3、10、18、27 | 分配/零分配标注清晰 ✓ | 无 |

---

## 健壮性问题

| 行号 | 问题描述 | 建议 |
|------|----------|------|
| 19–34 | 双层循环直接索引，越界即 panic（RT 路径）——调用方（process.rs）已先校验容量 | 加 `debug_assert!(frames*channels <= min_len)` 双保险 |
| 49 | 逐采样 `is_finite()` 正常路径零开销（编译器折叠）✓ | 无 |

---

## 安全问题

| 行号 | 问题描述 | 建议 |
|------|----------|------|
| — | 无 unsafe/FFI | 无 |

---

## 可维护性问题（含依赖违规）

| 行号 | 问题描述 | 建议 |
|------|----------|------|
| — | 无依赖 —— 与规范 4.4（无依赖）**一致** ✓ | 无 |

### 依赖违规检查

- 本文件所在模块：`pipeline/interleave.rs`
- 规范允许依赖：无
- 规范禁止依赖：install/config/object
- 实际依赖：无
- 违规项：无 ✅

---

## 优雅性建议

| 行号 | 当前写法 | 更 Rustacean 的写法 |
|------|----------|---------------------|
| 19–34 | 手写双层循环 | 已是最优形态（编译器向量化）；可考虑 `zip` 但会引入边界检查，保持现状 |
| 40–52 | guarded 版本与普通版本仅差一行 | 可抽公共循环 + `map` 处理，但内联性能优先，保持现状 |

---

## 优先级汇总

| 优先级 | 数量 | 说明 |
|--------|------|------|
| 🔴 阻断级 | 0 | 无 |
| 🟡 建议级 | 0 | 无 |
| 🟢 优化级 | 1 | debug_assert 容量双保险 |
