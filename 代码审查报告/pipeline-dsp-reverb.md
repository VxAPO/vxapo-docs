# 代码审查报告：pipeline/dsp/reverb.rs

- 审查日期：2026-08-11
- 被审文件：`D:\APO_Project\VxAPO\vxapo-driver\src\pipeline\dsp\reverb.rs`
- 审查模块：`pipeline/dsp/reverb`（Dattorro 板式混响）
- 参照规范：`pipeline 模块规范.md` 4.22（v9.3）、主规范 RT 约束
- 总体评价：⚠️ 需修改（轻微）

---

## 审查摘要

- 论文级实现：延迟表/抽头表/交叉反馈与 Dattorro 1997 一致、引用来源（论文 + GPL/MIT 对照）完整、RT 零分配（全部 initialize 预分配）、调制 AllPass 的缓冲余量计算正确、输出有限护栏；测试覆盖静音/直通/跨采样率/单声道。

---

## 可读性问题

| 行号 | 问题描述 | 建议 |
|------|----------|------|
| 1–32 | 算法与参数语义注释详实 ✓ | 无 |
| 71–73 | `DAT_TAP_MAX` 的用途注释有价值 ✓ | 无 |

---

## 健壮性问题

| 行号 | 问题描述 | 建议 |
|------|----------|------|
| 238–245 | `read_frac` 的 `d+1` 经 `read()` 取模，无越界 ✓ | 无 |
| 527–540 | `max_exc = lfo_depth.ceil()+2` 覆盖调制深度余量 ✓ | 无 |
| 544–551 | D1 缓冲 `base.max(tap_max)+2`——小 RoomSize 下抽头仍可读 ✓ | 无 |
| 657–658 | 交叉反馈 `right_sum = left*loop_gain`——注意 `left` 变量已含上一轮 loop_gain（642 行），此处再乘一次为论文语义 ✓ | 建议加注释避免误读 |
| 613–688 | 未实现 `latency()`（默认 0）：Reverb 实际延迟数百采样且隐藏——与全局“不上报延迟”策略一致 | 注释明确“隐藏延迟，不上报” |
| 113–196 | `parse_reverb_params` 疑似旧命令解析遗留（同其它效果器） | grep 确认 |

---

## 安全问题

| 行号 | 问题描述 | 建议 |
|------|----------|------|
| — | 无 unsafe/FFI | 无 |

---

## 可维护性问题（含依赖违规）

| 行号 | 问题描述 | 建议 |
|------|----------|------|
| 34 | 依赖仅 `dsp/filter` —— 符合 dsp/\*.rs 约束 ✓ | 无 |
| 36–76 | 论文常量集中、带 Table 编号 ✓ | 无 |
| 469–479 | `tap_line` 用 match 默认分支映射 id 5——`id` 来自常量表，语义隐晦 | 用 `(0..=5)` 显式 match 或 `match id { 0..=3 => ..., _ => ... }` 加注释 |

### 依赖违规检查

- 本文件所在模块：`pipeline/dsp/reverb.rs`
- 规范允许依赖：`dsp/filter`、`dsp/model`、`dsp/biquad`、`utils`
- 规范禁止依赖：config/install/object
- 实际依赖：`dsp/filter`
- 违规项：无 ✅

---

## 优雅性建议

| 行号 | 当前写法 | 更 Rustacean 的写法 |
|------|----------|---------------------|
| 583–592 | `map_taps` 闭包清晰 ✓ | 无 |
| 638–654 | 左右槽两段对称代码 | 可抽 `fn tank_side(...)` 但借用复杂；保持现状并注释 |

---

## 优先级汇总

| 优先级 | 数量 | 说明 |
|--------|------|------|
| 🔴 阻断级 | 0 | 无 |
| 🟡 建议级 | 2 | 隐藏延迟注释；parse 死代码确认 |
| 🟢 优化级 | 2 | tap_line 显式 match；交叉反馈注释 |
