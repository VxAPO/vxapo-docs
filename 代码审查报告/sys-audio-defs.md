# 代码审查报告：sys/audio_defs.rs

- 审查日期：2026-08-11
- 被审文件：`D:\APO_Project\VxAPO\vxapo-driver\src\sys\audio_defs.rs`
- 审查模块：`sys/audio_defs`（通道掩码常量 + 名称映射）
- 参照规范：`sys 模块规范.md` 3.5、`模块引用规范（无详细模块版）.md` 第十一节
- 总体评价：⚠️ 需修改（轻微）

---

## 审查摘要

- KS 位标志/标准布局常量带 ksmedia.h 来源说明、映射严格按位、未命中位不补；测试覆盖全位值/布局/名称。

---

## 可读性问题

| 行号 | 问题描述 | 建议 |
|------|----------|------|
| 111–119 | `get_channel_names` 文档写“掩码为 0 时按通道数兜底后重试”，实现直接返回空——文档/实现不一致 | 修正文档或实现按通道数兜底（需调用方传通道数） |
| 115–141 | MAPPINGS 表 ✓ | 无 |

---

## 健壮性问题

| 行号 | 问题描述 | 建议 |
|------|----------|------|
| 100–109 | `default_channel_mask(3/5/7/0)` 返回 0——兜底链终点可能为 0 | 与 install-device-format 同款问题；确认调用方是否接受 0 掩码 |
| 116–119 | mask==0 返回空列表——调用方（LockForProcess）在 mask==0 且有通道数时需自行兜底 | 可提供 `get_channel_names_for(channels, mask)` 双参版本 |
| 125–134 | 仅 8 个基础位；7.1 无 FLoC/FRoC 等扩展位名 | 可接受（当前通道集固定） |

---

## 安全问题

| 行号 | 问题描述 | 建议 |
|------|----------|------|
| — | 无 unsafe/FFI | 无 |

---

## 可维护性问题（含依赖违规）

| 行号 | 问题描述 | 建议 |
|------|----------|------|
| — | 实际零依赖（未用 windows-rs）——规范行“windows-rs”属过列 | 无 |
| 18–19 | SPEAKER 常量补全原因注释 ✓ | 无 |

### 依赖违规检查

- 本文件所在模块：`sys/audio_defs.rs`
- 规范允许依赖：windows-rs
- 规范禁止依赖：所有其他
- 实际依赖：无
- 违规项：无 ✅

---

## 优雅性建议

| 行号 | 当前写法 | 更 Rustacean 的写法 |
|------|----------|---------------------|
| 115–141 | 手工 filter/map | 已最简 |
| 100–109 | match 通道数 | 可 `match` 保持（最清晰） |

---

## 优先级汇总

| 优先级 | 数量 | 说明 |
|--------|------|------|
| 🔴 阻断级 | 0 | 无 |
| 🟡 建议级 | 2 | 文档/实现不一致（mask=0 兜底）；default_mask 0 值语义确认 |
| 🟢 优化级 | 0 | 无 |
