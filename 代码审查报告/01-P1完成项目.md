# 代码审查整改 — P1 完成项目（v9.17）

- 审查基准：2026-08-11（`00-索引.md`，代码基线 `VxAPO/vxapo-driver`，规范基准 v9.16）
- 完成基线：v9.17（2026-08-11）——driver `c05efde` / docs `6d1ac4e`
- 验收：`cargo test` 435 passed / 0 failed（注册表类测试在真实环境验证）、
  `cargo build --release` 零警告、`scripts/check_deps.ps1` 70 文件 0 违规

## 一、阻断级问题：13/13 完成

| # | 位置 | 问题 | 落地（v9.17） |
|---|------|------|---------------|
| 1 | `object/apo/process.rs` 588/842 | RT 每帧 `Box::new(Chain::new())` 占位分配（R2） | 字段拆借用，消除占位链与放回 |
| 2 | `object/apo/process.rs` 718/732 | 过渡路径 `Vec::resize` 潜在分配（R2） | 容量断言 + `[..n].fill(0.0)`，过渡缓冲保留长度 |
| 3 | `object/apo/process.rs` 多处 | unsafe 无 Safety 注释 + 切片长度未校验（R3） | 新增 `checked_interleaved_slice(_mut)`，逐处 SAFETY 注释 + 帧数 clamp |
| 4 | `object/apo.rs` 176–177 | `unsafe impl Send/Sync` 无 Safety 注释（R3） | 补规范 7.1 SAFETY 原文 |
| 5 | `object/apo/config.rs` 333–353 | 持 inner 锁执行磁盘 I/O（R5 边界） | `diag_append` 全部移出锁外，`diag.log` 1MB 轮转 |
| 6 | `object/apo/config.rs` | `reloading` 防覆盖从未置位（R2 规范偏差） | 短锁检查后立即置位 + RAII 复位 |
| 7 | `install/selector/operation.rs` 126–127 | 事务回滚 DeleteKey 空操作 | 改 `delete_tree(root, path)`（新增回滚单测） |
| 8 | `install/selector/operation.rs` 641–643 | 信息区创建无回滚记录 | `write_child_apo_config` 登记 `DeleteKey` |
| 9 | `object/factory.rs` 80 | 空指针先解引用后校验（UB） | 先校验 `riid/ppvobject` 再写 `*ppvobject` |
| 10 | `object/factory.rs` 34–37 | `lock_decrement` 零值下溢 | CAS 零值保护（对齐 ref_count.rs，新增回归测试） |
| 11 | `object/apo/aggregate.rs` 86–91 | x64 偏移无编译期护栏 | `const _: () = assert!(size_of::<*const ()>() == 8)` |
| 12 | `pipeline/process.rs` 100–137 | 防御检查被“先构造切片”架空 | 先 clamp `valid_frame_count ≤ max_frame_count` 再建切片 |
| 13 | `sys/registry.rs` 45–46 | `unsafe impl Send/Sync` 无 Safety 注释（R3） | 补 SAFETY 注释 + 全文件 unsafe 注释补齐 |

## 二、通用发现完成项

| 通用发现 | 状态 | 落地 |
|----------|------|------|
| 规范总表普遍滞后 | ✅ 完成 | 主规范 v9.17 第十一节按实际代码补录（含脚本新发现的 audiodg/select/state/apo_types 等缺口）；四份子规范引用约束总表改为指向主规范（单一事实源） |
| 旧命令解析器死代码（`parse_*_params`） | ✅ 完成 | 删除 `aural/loudness/maximizer/wide/reverb` 生产函数及旧测试（模型参数结构保留） |
| debug 探针残留（10+ 处） | ✅ 完成 | 全部 `*_probe.txt` 写盘探针删除（含 dll_exports/init 两处） |
| 控制型 COM 入口无 catch_unwind（R4） | ⚠️ 部分完成 | `Reset/GetLatency/GetInputChannelCount/LockForProcess/UnlockForProcess` 已兜底；**`Initialize` 未包，见 P2** |
| sys/registry 质量洼地（`.reg` 导出 + `delete_sub_key`） | ✅ 完成 | MULTI_SZ 改 UTF-16LE hex(7) 双终止、QWORD 完整 8 字节 hex(b)、根头按 root 输出、读值失败记警告；`delete_sub_key` 改相对句柄语义并修正调用方；补 `write_qword` |
| pipeline DSP RT 无分配结构保证（fir/maximizer） | ❌ 未做 | 见 P2 |
| telemetry 接线缺失（log + panic hook） | ❌ 未做 | 见 P2 |

## 三、建议稿（依赖流方向与APP接口暴露层级建议）落地部分

| 建议稿条目 | 状态 |
|-----------|------|
| P0：按 3.2 清单修订总表 | ✅ 完成（v9.17 主规范第十一节） |
| P0：sys/registry `.reg` 导出 + `delete_sub_key`（连带 install 事务回滚） | ✅ 完成 |
| P1：依赖校验脚本 D1–D7 | ✅ 完成（`scripts/check_deps.ps1`，70 文件 0 违规） |
| P1：内部模块可见性收敛 + api 薄层 | ❌ 暂缓（APP 未完成，按需再定，见 P2） |
| P2：交互逻辑迁出 driver / telemetry 接线 | ❌ 暂缓（见 P2） |
| P3：拆 crate | ❌ 长期预留（见 P2） |

## 四、新增回归测试

- `sys::registry::tests::reg_export_qword_multi_sz_roundtrip`（`.reg` 导出 round-trip）
- `sys::registry::tests::delete_sub_key_uses_relative_child`（相对子键语义）
- `install::selector::operation::tests::transaction_rollback_deletes_recorded_key`（回滚真实删除）
- `object::factory::tests::lock_decrement_zero_is_noop`（零值不下溢）
- `pipeline::process::tests::process_audio_clamps_frames_to_max_frame_count`（引擎违约截断）

## 五、范围说明

- 本清单只统计 `00-索引.md`（v9.16 审查基准）对应整改项；v9.17 之后的版本
  （v9.18 config 顶层 enabled 总开关、PEQ shelf 等）与审查整改无关，不在此列。
- 各审查报告的 🟡 建议级 / 🟢 优化级条目统一归入 `02-P2待完成项目.md`。
