# 模块引用规范文档 v6.9

## 一、层级总则

```
sys/          → 只依赖 windows-rs，不知道任何业务概念
pipeline/     → 依赖 sys/ + utils/，不知道 install/config/object
install/      → 依赖 sys/ + utils/ + object/vx_reg_props.rs，不知道 pipeline/config
config/       → 依赖 sys/ + utils/ + pipeline/dsp/filter.rs + pipeline/dsp/factory.rs，不知道具体 Filter 实现和 install/object
object/       → 依赖所有模块（胶水层，被 Windows 直接调用）
utils/        → 不依赖任何其他模块
telemetry/    → 依赖 pipeline/realtime/ring.rs
```


## 二、完整模块树

```
src/
├── lib.rs
├── sys.rs
├── pipeline.rs
├── install.rs
├── config.rs
├── object.rs
├── telemetry.rs
├── utils.rs
│
├── sys/
│   ├── com.rs
│   ├── registry.rs
│   ├── audio_defs.rs
│   │
│   └── com/
│       ├── prelude.rs
│       ├── apo_interfaces.rs
│       └── apo_types.rs
│
├── pipeline/
│   ├── context.rs
│   ├── buffer.rs
│   ├── format.rs
│   ├── chain.rs
│   ├── process.rs
│   ├── interleave.rs
│   ├── realtime.rs
│   ├── dsp.rs
│   │
│   ├── realtime/
│   │   ├── ring.rs
│   │   └── contract.rs
│   │
│   └── dsp/
│       ├── filter.rs
│       ├── factory.rs
│       ├── transition.rs
│       ├── biquad.rs
│       ├── peq.rs
│       ├── hp_lp.rs
│       ├── gain.rs
│       ├── delay.rs
│       ├── copy.rs
│       ├── graphic_eq.rs
│       ├── convolution.rs
│       ├── vst.rs
│       └── loudness.rs
│
├── install/
│   ├── device.rs
│   ├── selector.rs
│   ├── audiodg.rs
│   │
│   ├── device/
│   │   ├── endpoint.rs
│   │   ├── format.rs
│   │   ├── slots.rs
│   │   └── info.rs
│   │
│   └── selector/
│       ├── select.rs
│       └── operation.rs
│
├── config/
│   ├── parser.rs
│   ├── error.rs
│   ├── watcher.rs
│   ├── commands.rs
│   │
│   └── commands/
│       ├── channel.rs
│       ├── cond.rs
│       ├── device.rs
│       ├── expr.rs
│       ├── include.rs
│       ├── stage.rs
│       ├── graphic.rs
│       ├── preamp.rs
│       ├── copy.rs
│       ├── delay.rs
│       ├── filter.rs
│       └── rew.rs
│
├── object/
│   ├── apo.rs
│   ├── child.rs
│   ├── factory.rs
│   ├── ref_count.rs
│   ├── vx_reg_props.rs
│   └── dll_exports.rs
│
├── telemetry/
│   ├── logger.rs
│   └── panic.rs
│
└── utils/
    ├── align.rs
    └── vx_error.rs
```


## 三至十、详细规范逐模块分化

详见各子 md 文件：
- `三-sys.md`
- `四-pipeline.md`
- `五-install.md`
- `六-config.md`
- `七-object.md`
- `八-utils.md`
- `九-telemetry.md`
- `十-线程安全模型.md`


## 十一、引用约束总表

| 模块 | 可依赖 | 不可依赖 |
|------|--------|----------|
| `sys/audio_defs.rs` | windows-rs | 所有其他 |
| `sys/com/prelude.rs` | windows-rs | 所有其他 |
| `sys/com/apo_interfaces.rs` | prelude、apo_types | 其他 |
| `sys/com/apo_types.rs` | windows-rs | 其他 |
| `sys/registry.rs` | windows-rs、sys/com/prelude（guid_to_string） | 其他 |
| `pipeline/context.rs` | 无 | install/config/object |
| `pipeline/buffer.rs` | sys/com/apo_types | install/config/object |
| `pipeline/format.rs` | sys/com/apo_interfaces、sys/com/apo_types | install/config/object |
| `pipeline/interleave.rs` | 无 | install/config/object |
| `pipeline/chain.rs` | dsp/filter、utils | install/config/object、dsp/transition |
| `pipeline/process.rs` | context、chain、buffer、interleave、dsp/filter、dsp/transition、sys/com/apo_types、utils | install/config/object |
| `pipeline/realtime/contract.rs` | core | 其他 |
| `pipeline/realtime/ring.rs` | core | 其他 |
| `pipeline/dsp/filter.rs` | utils | config/install/object |
| `pipeline/dsp/factory.rs` | dsp/filter、dsp/*（仅为注册实例化具体类型的必要例外）、utils | config/install/object |
| `pipeline/dsp/transition.rs` | 无 | config/install/object |
| `pipeline/dsp/*.rs`（具体 Filter） | dsp/filter、dsp/biquad（如需）、utils | **config/（禁止 config 直接引用）** |
| `install/device/endpoint.rs` | sys/registry、utils/error | pipeline/config |
| `install/device/format.rs` | sys/registry、utils/error、sys/audio_defs（仅 default_channel_mask） | config |
| `install/device/slots.rs` | sys/registry、utils/guid、sys/com/prelude（guid_to_string） | pipeline/config |
| `install/device/info.rs` | device/endpoint、device/format、device/slots、sys/registry、object/vx_reg_props、utils/error | pipeline/config |
| `install/selector.rs` | selector/select、selector/operation（入口聚合） | pipeline/config |
| `install/selector/select.rs` | device/info（enumerate_devices）、utils/error | pipeline/config、sys/registry（不得自行遍历） |
| `install/selector/operation.rs` | device/slots、device/format、sys/registry、object/vx_reg_props、sys/com/prelude（guid_to_string）、utils/error | pipeline/config |
| `install/audiodg.rs` | sys/registry | pipeline/config/object |
| `config/parser.rs` | config/error、config/commands/*、pipeline/dsp/filter、pipeline/dsp/factory、sys/registry | **dsp/peq 等具体类型**、install/object、pipeline/process/chain/context、sys/audio_defs |
| `config/watcher.rs` | config/error、utils | install/object |
| `config/commands.rs` | config/commands/*、pipeline/dsp/factory、pipeline/dsp/register_builtin_filters | 其他 |
| `config/commands/*.rs` | config/error、config/parser(ParseContext)、dsp/factory、dsp/filter | **dsp/peq 等具体类型**、pipeline/chain、sys/audio_defs |
| `object/apo.rs` | **所有模块**（含 transition、telemetry/logger、sys/audio_defs、pipeline/dsp/filter[DspContext]） | 无 |
| `object/child.rs` | sys/com/prelude、sys/com/apo_interfaces、sys/com/apo_types | object/apo（禁止循环） |
| `object/factory.rs` | sys/com/prelude、object/apo、vx_reg_props、ref_count | 无 |
| `object/ref_count.rs` | core | 无 |
| `object/vx_reg_props.rs` | sys/com/prelude、sys/com/apo_types | 无 |
| `object/dll_exports.rs` | object/*、sys/com/prelude、utils、telemetry | 无 |
| `utils/align.rs` | 无 | 其他所有 |
| `utils/vx_error.rs` | windows-core | 其他所有 |
| `telemetry/logger.rs` | pipeline/realtime/ring | install/config/object |
| `telemetry/panic.rs` | telemetry/logger | install/config/object |


## 十二、禁止依赖链检查

| 源模块 | 禁止依赖 | 说明 |
|--------|----------|------|
| `sys/` | 任何其他模块 | FFI 层不知道业务 |
| `pipeline/` | `install/`、`config/`、`object/` | 运行时不知道安装和配置 |
| `pipeline/chain.rs` | `pipeline/dsp/transition.rs` | 过渡状态机由 object 层管理 |
| `pipeline/context.rs` | `pipeline/dsp/filter.rs`（DspContext） | context.rs 不引用 dsp/ 类型 |
| `pipeline/dsp/filter.rs` | `pipeline/context.rs`（PipelineContext） | filter.rs 不引用 context.rs 的类型，由调用方直接构造 DspContext |
| `install/` | `pipeline/`、`config/` | 安装不知道运行时和配置 |
| `config/` | `install/`、`object/`、`pipeline/process.rs`、`pipeline/chain.rs`、`pipeline/context.rs` | 配置解析不知道安装和运行时调度 |
| `config/commands/stage.rs` | `pipeline/dsp/peq.rs` | 必须通过 FilterRegistry 动态创建 |
| `config/commands/filter.rs` | `pipeline/dsp/biquad.rs` | 同上 |
| `config/commands/rew.rs` | `pipeline/dsp/peq.rs` | 同上 |
| `telemetry/logger.rs` | `alloc::string::String` | 实时路径禁止堆分配（定长数组） |
| `utils/` | 任何其他模块 | 工具层不能反向依赖 |


## 十三、数据流向

### 安装流程

```
selector/select.rs（用户选择）
  → device/info.rs::enumerate_devices（查询 DeviceInfo，设备枚举唯一入口）
  → selector/operation.rs::install_endpoint（7 步安装，含 Transaction 事务回滚）
      → audiodg.rs（检查 DisableProtectedAudioDG）
      → sys/registry.rs（写注册表 + save_to_file 备份）
      → object/vx_reg_props.rs（读 CLSID）
```

### 配置加载（LockForProcess）

```
object/apo.rs::LockForProcess
  → mutex.lock()
  → state.transition(Initialized → Locked)
  → RAII guard 创建（失败时自动回退）
  → pipeline/format.rs（从 IAudioMediaType 提取格式）
  → 构建 DspContext（纯数据，只读）
  → config/parser.rs::ConfigParser.parse_file(path, &dsp_ctx)
      → config/commands/*.rs（匹配命令）
          → registry.try_create(args, &dsp_ctx, loader)
  → pipeline/chain.rs::Chain::new() + add_filter × N
  → pipeline/context.rs（保存 PipelineContext）
  → 预分配 temp_buffers / temp_buffer_old / temp_buffer_new
  → latency_samples.store(chain.total_latency())
  → latency_frames_atomic.store(chain.total_latency())
  → ensure_can_load()
  → guard.disarm()
  → mutex.unlock()
```

### 配置热重载（watcher 触发，v6.9 R2 阻塞式）

```
config/watcher.rs（检测 config.txt 变化）
  → object/apo.rs::hot_reload
      → 短锁检查 transition.is_some() || reloading
          ├─ 是 → 直接返回（阻塞式：过渡期间不构建新链）
          └─ 否 → 锁外 ConfigParser.parse_file（构建新 Chain）
      → mutex.lock()
      → 竞态兜底：若解析期间新过渡已启动 → pending_reload = true 并返回
      → 旧 Chain 放入 outgoing_chain，新 Chain 设为 current_chain
      → transition = Some(SmoothingProvider::new(10ms))
      → reloading = false
      → mutex.unlock()

APOProcess 过渡完成帧（RT）：
  → retired_chain = outgoing_chain.take()   // R1：旧链移入退役槽，零析构
  → 若 pending_reload && !reloading → 释放锁后触发 hot_reload
```

### 运行时处理（含双链过渡）

```
Windows 音频引擎
  → object/apo.rs::APOProcess
      → state.current() 检查 == Locked
      → mutex.lock()
      → 检查 outgoing_chain 是否存在

      ├─ 【正常模式】
      │   → process_audio(props, params, current_chain, stats, temp_buffers)
      │       → evaluate_buffer → 去交织 → is_silent 检测 → mono 上混
      │       → chain.process()（去交织空间）
      │       → 交织 → apply_error_policy（失败时按策略恢复，计数器 +1）
      │
      └─ 【过渡模式】
          → 从 input_props[0] 提取输入切片
          → process_chain_interleaved(outgoing) → temp_buffer_old
          → apply_error_policy（失败时按策略恢复）
          → process_chain_interleaved(current_chain) → temp_buffer_new
          → apply_error_policy（失败时按策略恢复）
          → transition.advance() → factor
          → mix_buffers(old, new, output, factor)（交织空间混合）
          → factor >= 1.0 时 drop outgoing_chain，清除 transition
                → 若 pending_reload → 释放锁 → hot_reload()
      → mutex.unlock()
```


## 十四、关键设计决策摘要

### Chain 不拥有缓冲区

Chain 仅持有 `Vec<Box<dyn Filter>>` 和 `total_latency`。缓冲区由 `object/apo.rs` 的 `ApoObjectInner` 在 `LockForProcess` 时预分配并持有。Chain 的 `process()` 为纯计算操作（逐 Filter 调用），无锁、无分配、无 I/O。

### 双链过渡在 object 层管理

`SmoothingProvider` 仅提供升余弦混合因子（纯算法）。链状态（`current_chain` / `outgoing_chain` / `transition` / `pending_reload`）由 `ApoObjectInner` 管理。`process_chain_interleaved` 桥接函数封装交织→去交织→处理→交织的完整流程，供过渡模式对新旧两个 Chain 各调用一次后混合。

### DspContext 由调用方构造

`pipeline/dsp/filter.rs` 定义 `DspContext` 结构体（纯数据），不依赖 `pipeline/context.rs` 的 `PipelineContext`。由 `object/apo.rs` 在 `LockForProcess` 中从 `PipelineContext` + 额外参数直接构造 `DspContext`，传给 `ConfigParser`。

### config/ 不直接操作 Chain

`config/parser.rs` 返回 `Vec<Box<dyn Filter>>`，由 `object/apo.rs` 负责添加到 Chain。`Channel:` 命令通过 `Filter.initialize()` 返回 `Some(channel_names)` 触发通道选择，不直接操作 Chain 的通道管理接口。

### 线程安全模型

- `self.mutex`（`Mutex<ApoObjectInner>`）：保护 Chain、过渡状态、PipelineContext、临时缓冲区
- `self.ap_state`（`Mutex<ApoObjectState>`）：保护格式/通道/锁定状态
- `self.state_cell`（`AtomicU8` + CAS）：无锁状态机（Created → Initialized → Locked）
- `self.latency_samples` / `self.latency_frames_atomic`（`AtomicU32`）：免锁延迟值
- 两锁不交叉持有，`CalcInputFrames` / `CalcOutputFrames` 通过原子变量免锁读取


## 十五、生产构建约束（v6.6，O3）

面向 Windows APO 生产的 **release 构建硬性要求**（tympan-apo 借鉴，底线）：

```toml
[profile.release]
panic = "abort"      # 必须：RT 路径 panic 若 unwind 会跨 FFI 传播（UB 风险）
codegen-units = 1    # 必须：保证内联/代码布局可预测，RT 抖动最小化
lto = "thin"         # 建议：减体积 + 跨 crate 内联
strip = "debuginfo"  # 建议：生产 DLL 体积
```

- **RT 路径禁止 unwind**：所有实时路径回调（`APOProcess`/`Filter::process`/`accept_aux_input`）不得展开栈。
  `panic = "abort"` 将其变为硬边界——panic 即进程终止，绝不跨 `extern "system"` FFI 边界 unwind。
- 开发/测试 profile 可 `panic = "unwind"`（便于测试 catch_unwind 的防御路径），但**生产 release 必须 abort**。

## 关键设计决策摘要补记（v6.6，tympan-apo 借鉴）

| 编号 | 决策 | 落点 |
|------|------|------|
| O1 | **RT 编译期见证**：`RealtimeContext` 零尺寸标记 + `DspContext::rt_marker`（`PhantomData<RealtimeContext>`）——编译期能解决的问题绝不拖到运行时 | pipeline 4.7 / 4.9 |
| O2 | **StateCell 补全**：`release()`（任意态→Created，析构终态复位）+ `TransitionError{expected, attempted, actual}` + 语义化转换（initialize/lock/unlock） | object 7.1.4 |
| O3 | **生产构建约束**：release 必须 `panic="abort"` + `codegen-units=1`（RT 跨 FFI unwind = UB） | 本章十五 |
| O4 | **AEC 接口预留**：`feature = "aec"` 门控 3 个 AEC 接口 + IID 常量（不实现，声明位置） | sys 3.2 |
| E1 | **就地处理声明**（EqualizerAPO `IFilter::getInPlace` 借鉴）：`Filter::is_in_place()`（默认 true）+ `Chain::is_fully_in_place()`——全链就地时 `temp_buffers` 即最终输出，零拷贝快路径 | pipeline 4.9 / 4.5 / 4.6 |
| E3.1 | **默认设备判定边界**：driver 层（`enumerate_devices`）仅返回合法安装容器，不判定默认设备；默认/有效设备由用户层经 COM `GetDefaultAudioEndpoint` 取得后与 driver 结果对应 | install 5.4 |
| E3.2 | **设备物理状态谓词**（EAPO `isDisabled/isUnplugged` 借鉴）：`DeviceInfo::is_disabled()/is_unplugged()` 由已有 `EndpointState` 推导，零新增 I/O | install 5.4 |
| E3.3 | **autoAdjust 独立字段**（EAPO `InstallState::autoAdjust` 借鉴）：`InstallConfig::auto_adjust`（默认 false），Step 4 写注册表读取该字段而非硬编码 | install 5.5.2 |
| E3.4 | **安装自检**（EAPO `testAPOInstallation` 借鉴）：`install_endpoint(..., verify)`——commit 后 `CoCreateInstance` 验证 DLL 可实例化；失败不自动回滚，报告+用户决策 | install 5.5.2 |
| R1 | **退役链延迟析构**（EAPO `previousConfig` 借鉴）：过渡完成帧 RT 线程仅 `retired_chain = outgoing_chain.take()`（零析构），控制线程在锁内统一 drop——重型滤波器析构绝不留在 RT 线程 | object 7.1.3/7.1.9/7.1.10/7.1.11/7.1.13 |
| R2 | **阻塞式重载**（EAPO `loadSemaphore` 对齐）：hot_reload 检测到过渡在途/加载中即返回不构建；过渡完成由 APOProcess 触发重载。`reloading` 标志防覆盖——消除排队式重复解析与覆盖漏洞 | object 7.1.3/7.1.11/7.1.18 |
| R3 | **空链快路径**（EAPO `isEmpty` 借鉴）：`Chain::is_empty()` 时 `process_audio` 直接复制去交织结果（近似 memcpy），跳过链遍历；与 E1 就地快路径叠加 | pipeline 4.5/4.6 |
| R4 | **过渡周期 10ms**（EAPO 对齐）：`default_smoothing_length` 由 `sample_rate/20`（50ms）→ `sample_rate/100`（10ms）——双处理窗口缩短 5 倍，"乱切也无所谓" | pipeline 4.11 |
