# 模块引用规范文档 v9.16

## 一、层级总则

```
sys/          → 只依赖 windows-rs，不知道任何业务概念
pipeline/     → 依赖 sys/ + utils/，不知道 install/config/object
install/      → 依赖 sys/ + utils/ + object/vx_reg_props.rs，不知道 pipeline/config
config/       → 依赖 sys/ + utils/ + pipeline/dsp/filter.rs + pipeline/dsp/model.rs + pipeline/dsp/factory.rs，不知道具体 Filter 实现和 install/object
object/       → 依赖所有模块（胶水层，被 Windows 直接调用）
utils/        → 不依赖任何其他模块
telemetry/    → 依赖 pipeline/realtime/ring.rs
```


## 二、完整模块树

```
src/
├── lib.rs
├── sys.rs
├── pipeline.rs
├── install.rs
├── config.rs
├── object.rs
├── telemetry.rs
├── utils.rs
│
├── sys/
│   ├── com.rs
│   ├── registry.rs
│   ├── audio_defs.rs
│   │
│   └── com/
│       ├── prelude.rs
│       ├── apo_interfaces.rs
│       └── apo_types.rs
│
├── pipeline/
│   ├── context.rs
│   ├── buffer.rs
│   ├── format.rs
│   ├── chain.rs
│   ├── process.rs
│   ├── interleave.rs
│   ├── realtime.rs
│   ├── dsp.rs
│   │
│   ├── realtime/
│   │   ├── ring.rs
│   │   └── contract.rs
│   │
│   └── dsp/
│       ├── filter.rs
│       ├── factory.rs
│       ├── transition.rs
│       ├── biquad.rs
│       ├── fir.rs           # FIR 基础设施（SIMD dot / 分块 FFT，v9.11）
│       ├── model.rs         # ChainModel / EffectType（v9.11）
│       ├── peq_hybrid.rs    # 混合式 PEQ（v9.11）
│       ├── gain.rs
│       ├── math.rs
│       ├── loudness.rs
│       ├── aural.rs          # 效果器（Reverb v9.3 / Maximizer v9.8 / Aural v9.9 / Wide v9.10 FIR / PEQ v9.11）
│       ├── maximizer.rs
│       ├── reverb.rs
│       └── wide.rs
│
├── install/
│   ├── device.rs
│   ├── selector.rs
│   ├── audiodg.rs
│   │
│   ├── device/
│   │   ├── endpoint.rs
│   │   ├── format.rs
│   │   ├── slots.rs
│   │   ├── sysfx.rs        # CAPX MSFX 模板定位/接管/恢复（v9.0）
│   │   └── info.rs
│   │
│   └── selector/
│       ├── select.rs
│       └── operation.rs
│
├── config/
│   ├── parser.rs
│   ├── model.rs            # FileModel（serde，v9.11）
│   ├── error.rs
│   ├── watcher.rs
│
├── object/
│   ├── apo.rs
│   ├── dll_exports.rs
│   ├── factory.rs
│   ├── ref_count.rs
│   ├── vx_reg_props.rs
│   │
│   └── apo/
│       ├── aggregate.rs
│       ├── child.rs
│       ├── config.rs
│       ├── init.rs
│       ├── inner.rs
│       ├── negotiate.rs
│       ├── process.rs
│       └── state.rs
│
├── telemetry/
│   ├── logger.rs
│   └── panic.rs
│
└── utils/
    ├── align.rs
    ├── guid.rs
    └── vx_error.rs
```


## 三至十、详细规范逐模块分化

详见各子 md 文件：
- `三-sys.md`
- `四-pipeline.md`
- `五-install.md`
- `六-config.md`
- `七-object.md`
- `八-utils.md`
- `九-telemetry.md`
- `十-线程安全模型.md`


## 十一、引用约束总表

| 模块 | 可依赖 | 不可依赖 |
|------|--------|----------|
| `sys/audio_defs.rs` | windows-rs | 所有其他 |
| `sys/com/prelude.rs` | windows-rs | 所有其他 |
| `sys/com/apo_interfaces.rs` | prelude、apo_types | 其他 |
| `sys/com/apo_types.rs` | windows-rs | 其他 |
| `sys/registry.rs` | windows-rs、sys/com/prelude（guid_to_string） | 其他 |
| `sys/known_folder.rs` | windows-rs | 其他 |
| `pipeline/context.rs` | 无 | install/config/object |
| `pipeline/buffer.rs` | sys/com/apo_types | install/config/object |
| `pipeline/format.rs` | sys/com/apo_interfaces、sys/com/apo_types | install/config/object |
| `pipeline/interleave.rs` | 无 | install/config/object |
| `pipeline/chain.rs` | dsp/filter、utils | install/config/object、dsp/transition |
| `pipeline/process.rs` | context、chain、buffer、interleave、dsp/filter、dsp/transition、sys/com/apo_types、utils | install/config/object |
| `pipeline/realtime/contract.rs` | core | 其他 |
| `pipeline/realtime/ring.rs` | core | 其他 |
| `pipeline/dsp/filter.rs` | utils | config/install/object |
| `pipeline/dsp/factory.rs` | dsp/filter、dsp/model（EffectConfig/EffectType）、dsp/*（静态 match 实例化，v9.11）、utils | config/install/object |
| `pipeline/dsp/transition.rs` | 无 | config/install/object |
| `pipeline/dsp/*.rs`（具体 Filter） | dsp/filter、dsp/model（v9.11）、dsp/biquad（loudness 内部用）、utils | **config/（禁止 config 直接引用）** |
| `install/device/endpoint.rs` | sys/registry、utils/error | pipeline/config |
| `install/device/format.rs` | sys/registry、utils/error、sys/audio_defs（仅 default_channel_mask） | config |
| `install/device/slots.rs` | sys/registry、utils/guid、sys/com/prelude（guid_to_string） | pipeline/config |
| `install/device/info.rs` | device/endpoint、device/format、device/slots、sys/registry、object/vx_reg_props、utils/error | pipeline/config |
| `install/device/sysfx.rs` | device/slots、object/vx_reg_props、sys/registry、sys/com/prelude、utils/error | pipeline/config |
| `install/selector.rs` | selector/select、selector/operation（入口聚合） | pipeline/config |
| `install/selector/select.rs` | device/info（enumerate_devices）、utils/error | pipeline/config、sys/registry（不得自行遍历） |
| `install/selector/operation.rs` | device/slots、device/sysfx、device/format、sys/registry、object/vx_reg_props、object/dll_exports（register_apo_with_path）、sys/com/prelude（guid_to_string）、utils/error | pipeline/config |
| `install/audiodg.rs` | sys/registry | pipeline/config/object |
| `config/parser.rs` | config/error、config/model、pipeline/dsp/model、pipeline/dsp/filter、pipeline/dsp/factory | **dsp/peq_hybrid 等具体类型**、install/object、pipeline/process/chain/context、sys/audio_defs |
| `config/model.rs` | config/error、pipeline/dsp/model、pipeline/dsp/{aural,maximizer,reverb,wide}（参数结构） | install/object、pipeline/process/chain |
| `config/watcher.rs` | config/error、utils | install/object |
| `config/commands.rs` | —（v9.11 删除） | — |
| `config/commands/*.rs` | —（v9.11 删除） | — |
| `object/apo.rs` | **所有模块**（含 transition、telemetry/logger、sys/audio_defs、pipeline/dsp/filter[DspContext]）；模块入口，聚合 `object/apo/*` | 无 |
| `object/apo/aggregate.rs` | sys/com/prelude、object/apo/child、object/apo/process | object/apo（禁止循环） |
| `object/apo/child.rs` | sys/com/prelude、sys/com/apo_interfaces、sys/com/apo_types | object/apo（禁止循环） |
| `object/apo/config.rs` | config/parser、pipeline/dsp/filter、pipeline/context、pipeline/chain、object/apo/state | object/apo（禁止循环） |
| `object/apo/init.rs` | sys/com/prelude、object/apo/config、object/apo/child | object/apo（禁止循环） |
| `object/apo/inner.rs` | pipeline/context、pipeline/chain、pipeline/dsp/transition、utils | object/apo（禁止循环） |
| `object/apo/negotiate.rs` | sys/com/apo_interfaces、sys/com/apo_types、pipeline/format、utils | object/apo（禁止循环） |
| `object/apo/process.rs` | object/apo、object/apo/config、object/apo/state、pipeline/*、config/parser、sys/com/apo_types、utils | object/apo（禁止循环） |
| `object/apo/state.rs` | core | object/apo（禁止循环） |
| `object/factory.rs` | sys/com/prelude、object/apo、vx_reg_props、ref_count | 无 |
| `object/ref_count.rs` | core | 无 |
| `object/vx_reg_props.rs` | sys/com/prelude、sys/com/apo_types | 无 |
| `object/dll_exports.rs` | object/*、sys/com/prelude、sys/registry（CLSID 键写入）、utils、telemetry | 无 |
| `utils/align.rs` | 无 | 其他所有 |
| `utils/guid.rs` | core | 其他所有 |
| `utils/vx_error.rs` | windows-core | 其他所有 |
| `telemetry/logger.rs` | pipeline/realtime/ring | install/config/object |
| `telemetry/panic.rs` | telemetry/logger | install/config/object |


## 十二、禁止依赖链检查

| 源模块 | 禁止依赖 | 说明 |
|--------|----------|------|
| `sys/` | 任何其他模块 | FFI 层不知道业务 |
| `pipeline/` | `install/`、`config/`、`object/` | 运行时不知道安装和配置 |
| `pipeline/chain.rs` | `pipeline/dsp/transition.rs` | 过渡状态机由 object 层管理 |
| `pipeline/context.rs` | `pipeline/dsp/filter.rs`（DspContext） | context.rs 不引用 dsp/ 类型 |
| `pipeline/dsp/filter.rs` | `pipeline/context.rs`（PipelineContext） | filter.rs 不引用 context.rs 的类型，由调用方直接构造 DspContext |
| `install/` | `pipeline/`、`config/` | 安装不知道运行时和配置 |
| `config/` | `install/`、`object/`、`pipeline/process.rs`、`pipeline/chain.rs`、`pipeline/context.rs` | 配置解析不知道安装和运行时调度 |
| `config/parser.rs`（v9.11） | `pipeline/dsp/factory.rs` | 经 `create_from_model` 静态分派，不直接触碰具体 Filter |
| `telemetry/logger.rs` | `alloc::string::String` | 实时路径禁止堆分配（定长数组） |
| `utils/` | 任何其他模块 | 工具层不能反向依赖 |


## 十三、数据流向

### 安装流程

```
selector/select.rs（用户选择）
  → device/info.rs::enumerate_devices（查询 DeviceInfo，设备枚举唯一入口）
  → selector/operation.rs::install_endpoint（7 步安装，含 Transaction 事务回滚）
      → audiodg.rs（检查 DisableProtectedAudioDG）
      → sys/registry.rs（写注册表 + save_to_file 备份）
      → object/vx_reg_props.rs（读 CLSID）
```

### 配置加载（LockForProcess）

```
object/apo.rs::LockForProcess
  → mutex.lock()
  → state.transition(Initialized → Locked)
  → RAII guard 创建（失败时自动回退）
  → pipeline/format.rs（从 IAudioMediaType 提取格式）
  → 构建 DspContext（纯数据，只读）
  → config/parser.rs::ConfigParser.parse_file(path, &dsp_ctx)（v9.11 TOML）
      → toml::from_str::<FileModel> → into_chain_model（校验）
          → factory::create_from_model（静态 match，构造 Filter 链）
  → pipeline/chain.rs::Chain::new() + add_filter × N + initialize(sample_rate, channel_names)
  → pipeline/context.rs（保存 PipelineContext）
  → 预分配 temp_buffers / temp_buffer_old / temp_buffer_new
  → latency_samples / latency_frames_atomic 恒 0（v9.12 不上报引擎）
  → latency_frames_atomic.store(chain.total_latency())
  → ensure_can_load()
  → guard.disarm()
  → mutex.unlock()
```

### 配置热重载（watcher 触发，v6.9 R2 阻塞式）

```
config/watcher.rs（检测 config.toml 变化）
  → object/apo.rs::hot_reload
      → 短锁检查 transition.is_some() || reloading
          ├─ 是 → 直接返回（阻塞式：过渡期间不构建新链）
          └─ 否 → 锁外 ConfigParser.parse_file_with_spec
                   （TOML → FileModel → ChainModel → create_from_model，
                    spec 指纹短路，v9.11）
      → mutex.lock()
      → 竞态兜底：若解析期间新过渡已启动 → pending_reload = true 并返回
      → 新 Chain initialize 后放入 current_chain，旧 Chain 放入 outgoing_chain
      → transition = Some(SmoothingProvider::new(10ms))
      → reloading = false
      → mutex.unlock()

APOProcess 过渡完成帧（RT）：
  → retired_chain = outgoing_chain.take()   // R1：旧链移入退役槽，零析构
  → 若 pending_reload && !reloading → 释放锁后触发 hot_reload
```

### 运行时处理（含双链过渡）

```
Windows 音频引擎
  → object/apo.rs::APOProcess
      → state.current() 检查 == Locked
      → mutex.lock()
      → 检查 outgoing_chain 是否存在

      ├─ 【正常模式】
      │   → process_audio(props, params, current_chain, stats, temp_buffers)
      │       → evaluate_buffer → 去交织 → is_silent 检测 → mono 上混
      │       → chain.process()（去交织空间）
      │       → 交织 → apply_error_policy（失败时按策略恢复，计数器 +1）
      │
      └─ 【过渡模式】
          → 从 input_props[0] 提取输入切片
          → process_chain_interleaved(outgoing) → temp_buffer_old
          → apply_error_policy（失败时按策略恢复）
          → process_chain_interleaved(current_chain) → temp_buffer_new
          → apply_error_policy（失败时按策略恢复）
          → transition.advance() → factor
          → mix_buffers(old, new, output, factor)（交织空间混合）
          → factor >= 1.0 时 drop outgoing_chain，清除 transition
                → 若 pending_reload → 释放锁 → hot_reload()
      → mutex.unlock()
```


## 十四、关键设计决策摘要

### Chain 不拥有缓冲区

Chain 仅持有 `Vec<Box<dyn Filter>>` 和 `total_latency`。缓冲区由 `object/apo.rs` 的 `ApoObjectInner` 在 `LockForProcess` 时预分配并持有。Chain 的 `process()` 为纯计算操作（逐 Filter 调用），无锁、无分配、无 I/O；`initialize()` 必须在首次 `process()` 前调用，否则依赖预计算状态的滤波器（GraphicEQ/PEQ/IIR/Delay/Convolution）会静默空转。

### 双链过渡在 object 层管理

`SmoothingProvider` 仅提供升余弦混合因子（纯算法）。链状态（`current_chain` / `outgoing_chain` / `transition` / `pending_reload`）由 `ApoObjectInner` 管理。`process_chain_interleaved` 桥接函数封装交织→去交织→处理→交织的完整流程，供过渡模式对新旧两个 Chain 各调用一次后混合。

### DspContext 由调用方构造

`pipeline/dsp/filter.rs` 定义 `DspContext` 结构体（纯数据），不依赖 `pipeline/context.rs` 的 `PipelineContext`。由 `object/apo.rs` 在 `LockForProcess` 中从 `PipelineContext` + 额外参数直接构造 `DspContext`，传给 `ConfigParser`。

### config/ 不直接操作 Chain

`config/parser.rs` 返回 `Vec<Box<dyn Filter>>`，由 `object/apo.rs` 负责添加到 Chain。`Channel:` 命令通过 `Filter.initialize()` 返回 `Some(channel_names)` 触发通道选择，不直接操作 Chain 的通道管理接口。

### 线程安全模型

- `self.mutex`（`Mutex<ApoObjectInner>`）：保护 Chain、过渡状态、PipelineContext、临时缓冲区
- `self.ap_state`（`Mutex<ApoObjectState>`）：保护格式/通道/锁定状态
- `self.state_cell`（`AtomicU8` + CAS）：无锁状态机（Created → Initialized → Locked）
- `self.latency_samples` / `self.latency_frames_atomic`（`AtomicU32`）：免锁延迟值
- 两锁不交叉持有，`CalcInputFrames` / `CalcOutputFrames` 通过原子变量免锁读取


## 十五、生产构建约束（v6.6，O3）

面向 Windows APO 生产的 **release 构建硬性要求**（tympan-apo 借鉴，底线）：

```toml
[profile.release]
panic = "abort"      # 必须：RT 路径 panic 若 unwind 会跨 FFI 传播（UB 风险）
codegen-units = 1    # 必须：保证内联/代码布局可预测，RT 抖动最小化
lto = "thin"         # 建议：减体积 + 跨 crate 内联
strip = "debuginfo"  # 建议：生产 DLL 体积
```

- **RT 路径禁止 unwind**：所有实时路径回调（`APOProcess`/`Filter::process`/`accept_aux_input`）不得展开栈。
  `panic = "abort"` 将其变为硬边界——panic 即进程终止，绝不跨 `extern "system"` FFI 边界 unwind。
- 开发/测试 profile 可 `panic = "unwind"`（便于测试 catch_unwind 的防御路径），但**生产 release 必须 abort**。

## 关键设计决策摘要补记（v6.6，tympan-apo 借鉴）

| 编号 | 决策 | 落点 |
|------|------|------|
| O1 | **RT 编译期见证**：`RealtimeContext` 零尺寸标记 + `DspContext::rt_marker`（`PhantomData<RealtimeContext>`）——编译期能解决的问题绝不拖到运行时 | pipeline 4.7 / 4.9 |
| O2 | **StateCell 补全**：`release()`（任意态→Created，析构终态复位）+ `TransitionError{expected, attempted, actual}` + 语义化转换（initialize/lock/unlock） | object 7.1.4 |
| O3 | **生产构建约束**：release 必须 `panic="abort"` + `codegen-units=1`（RT 跨 FFI unwind = UB） | 本章十五 |
| O4 | **AEC 接口预留**：`feature = "aec"` 门控 3 个 AEC 接口 + IID 常量（不实现，声明位置） | sys 3.2 |
| E1 | **就地处理声明**（EqualizerAPO `IFilter::getInPlace` 借鉴）：`Filter::is_in_place()`（默认 true）+ `Chain::is_fully_in_place()`——全链就地时 `temp_buffers` 即最终输出，零拷贝快路径 | pipeline 4.9 / 4.5 / 4.6 |
| E3.1 | **默认设备判定边界**：driver 层（`enumerate_devices`）仅返回合法安装容器，不判定默认设备；默认/有效设备由用户层经 COM `GetDefaultAudioEndpoint` 取得后与 driver 结果对应 | install 5.4 |
| E3.2 | **设备物理状态谓词**（EAPO `isDisabled/isUnplugged` 借鉴）：`DeviceInfo::is_disabled()/is_unplugged()` 由已有 `EndpointState` 推导，零新增 I/O | install 5.4 |
| E3.3 | **autoAdjust 独立字段**（EAPO `InstallState::autoAdjust` 借鉴）：`InstallConfig::auto_adjust`（默认 false），Step 4 写注册表读取该字段而非硬编码 | install 5.5.2 |
| E3.4 | **安装自检**（EAPO `testAPOInstallation` 借鉴）：`install_endpoint(..., verify)`——commit 后 `CoCreateInstance` 验证 DLL 可实例化；失败不自动回滚，报告+用户决策 | install 5.5.2 |
| R1 | **退役链延迟析构**（EAPO `previousConfig` 借鉴）：过渡完成帧 RT 线程仅 `retired_chain = outgoing_chain.take()`（零析构），控制线程在锁内统一 drop——重型滤波器析构绝不留在 RT 线程 | object 7.1.3/7.1.9/7.1.10/7.1.11/7.1.13 |
| R2 | **阻塞式重载**（EAPO `loadSemaphore` 对齐）：hot_reload 检测到过渡在途/加载中即返回不构建；过渡完成由 APOProcess 触发重载。`reloading` 标志防覆盖——消除排队式重复解析与覆盖漏洞 | object 7.1.3/7.1.11/7.1.18 |
| R3 | **空链快路径**（EAPO `isEmpty` 借鉴）：`Chain::is_empty()` 时 `process_audio` 直接复制去交织结果（近似 memcpy），跳过链遍历；与 E1 就地快路径叠加 | pipeline 4.5/4.6 |
| R4 | **过渡周期 10ms**（EAPO 对齐）：`default_smoothing_length` 由 `sample_rate/20`（50ms）→ `sample_rate/100`（10ms）——双处理窗口缩短 5 倍，"乱切也无所谓" | pipeline 4.11 |


## 十六、路线治理（v7.0）

### 路线清单驱动

**任何新功能、新需求必须先登记路线清单（`roadmap.md`），禁止"规范外直接实现"。**
`roadmap.md` 是"要做什么"的规划（规范性文件），与"怎么做"的规范文档配套。

- 条目字段：编号 / 状态 / 优先级 / 关联 Phase / 目标 / 影响模块 / 规范落点 / 依赖 / DoD
- 状态机：`Backlog → Spec-Drafting → Spec-Finalized → Implementing → Done`
- **硬门禁**：仅 `Spec-Finalized` 可进入 `Implementing`——实现之前，规范已对其引用约束、模块边界、RT 安全做过审查

### 三文件联动

一次版本变更必须同时满足（缺一不可）：

| 文件 | 动作 |
|------|------|
| `roadmap.md` | 任务状态更新（如 → `Spec-Finalized`）、规范落点回填 |
| 规范文档（本文件及子规范） | 对应章节落地、本文件版本号递增 |
| `changelog.md` | 新增记录（严格遵循 changelog-rule 记录格式） |

**一致性校验**：changelog 版本号 == 本文件版本号 == roadmap 条目规范落点引用版本。

### 配套规则

- 状态机与门禁细则：`.clinerules/roadmap-rule.md`
- 条目登记：`.clinerules/skills/roadmap-add-item/SKILL.md`
- 定稿审查（引用约束 / RT 安全 / 模块边界 + 三文件联动）：`.clinerules/skills/spec-finalize/SKILL.md`
- changelog 维护：`.clinerules/changelog-rule.md`


## 十七、实现反馈闭环（v7.4，v8.0 反馈落点外置）

执行端实现中发现的规范问题，经反馈后由规范侧修订，形成闭环：

```
实现（执行端）→ 发现规范不可行/遗漏 → feedback.md 新增记录（PX-X + 影响版本 + 问题 + 规范侧判定）
  → 规范侧核对反馈 → 修订规范（版本递增 + changelog）→ feedback.md 追加「修订记录」
  → 执行端继续实现 → 若再次发现问题 → 新增/追加同条记录
  → 修订全部落地 → feedback.md 状态标记「已修订」
  → 规范侧合规核对 → roadmap 条目标记 Done + 归档
```

**规则**：

1. **执行端不改状态字段**：实现完成只追加反馈；`Done` 由规范侧合规核对后标记（roadmap-rule）。
2. **零容忍绕过**：实现中发现规范不足，禁止"绕过去"继续编代码；必须反馈。
3. **反馈落点（v8.0）**：统一记录于 `feedback.md`（维护规则 `.clinerules/feedback-rule.md`）——
   **禁止写入 `roadmap.md`**（roadmap 只留引用 `> 反馈记录：feedback.md #PX-X`）。
4. **反馈类型**（对应 feedback.md「规范侧判定」）：
   - 规范文本矛盾/歧义 → 规范侧修订文本（如 v7.4 修订 `config 6.3` 注册意图澄清，`feedback.md #P0-2-1`）
   - 规范原型不可实现 → 更新原型（如 v7.4 `ParseContext.current_file: &'a Path` → `PathBuf`，`feedback.md #P0-2-2`）
   - 规范遗漏分发路径 → 补充（如 v7.4 REW `Filter N:` 动态命令名分支，`feedback.md #P0-2-3`）
5. **定稿记录**：修订伴随版本递增 + changelog（对应章节精确标注）+ feedback.md 修订记录。
6. **实现完成报告须含**：DoD 勾选 + RT/引用约束自查 + 实际触碰文件（⊆ 影响模块）+ 遗留问题（报告正文亦外置 feedback.md；roadmap 已完成归档保留核心落点）。


## 十八、EAPO 对齐度与差异化（v8.1，P0-6 子 APO 实现对齐）

> **目的**：明确 VxAPO 与 EqualizerAPO 在「子 APO」与「过渡」两大机制上的对齐度与差异化，以及差异化带来的设计影响。
> 依据：实读 EAPO `EqualizerAPO.cpp` / `FilterEngine.h/.cpp` / `FilterConfiguration.cpp`（2026-08-03）。

### 18.1 对齐度（EAPO 事实行为 == VxAPO 决策）

| # | 机制 | EAPO 事实（源码） | VxAPO 对齐决策 | 落点 |
|---|------|------------------|----------------|------|
| A1 | 子 APO 创建流程 | CoCreateInstance(IAPO) → QI RT → QI Cfg → Initialize 同传父 APOInit 数据（EqualizerAPO.cpp 186-215） | **完全对齐**：object/child.rs create 三步 + Initialize 透传 | object 7.1.8 / 7.2 |
| A2 | 创建失败降级 | 任一步失败 → resetChild（三接口 Release）→ **返回 S_OK 不阻塞父**（191-209） | **完全对齐**：child_apo=None 降级模式 | object 7.1.8 Note 57 |
| A3 | APOProcess 委托时序 | **childRT->APOProcess 先跑**（作用于输入缓冲）→ engine.process 处理其输出（472-478） | **完全对齐**：child 前置每帧一次，输出即 VxAPO 链输入 | object 7.2 / 7.1.11 |
| A4 | GetLatency 委托 | `*pTime=0` → **有 child 才委托** childAPO->GetLatency（91-94）；**无 child 返回 0** | **对齐（v8.3 修正）**：委托 child；无 child 返回 0（object 7.2「无 child 走自身」修正为「返回 0」） | object 7.2 |
| A5 | child 输出缓冲 | childRT->APOProcess 先跑作用于输入/输出缓冲（472-477）；**child 自身是否就地由 child 的 is_in_place 决定**，EAPO 不强制"无独立缓冲" | **对齐（v8.3 修正）**：child 按 is_in_place 语义；非就地需 `temp_buffer_child`（Lock 预分配，RT 零分配） | object 7.1.9 |

### 18.2 差异化（VxAPO 特有，EAPO 无）

> **前置：APO 链格式授予语义（v8.1 补正，18.2 机制根因）**——inFormat/outFormat 是父 APO 的**两个独立连接描述符**（引擎分别授予）：
> - **无 child**：父自己处理 in→out，父**拒绝下混**（IsInputFormatSupported 对 in>out 返回 S_FALSE + 提供 outFormat 为支持格式，277-282）+ **仅 mono→stereo 上混补做**（FilterConfiguration.cpp 126-128，补系统默认行为）——除该特例外协商后 in==out。
> - **有 child**：父在 LockForProcess 把 in/out 描述符**原样传给 child**（`childCfg->LockForProcess(ppInputConnections, ppOutputConnections)`）；
>   child **输入 = inFormat**（上游）、**输出 = outFormat**（下游/父要的）；child 负责就地 in→out 转换；
>   父真正处理的是 **child 输出后的缓冲 = out 布局**，故 `realChannelCount = outFormat`（处理维度 = 输出）。
> - **协商委托**：有 child 时父先问 `childAPO->IsInputFormatSupported`（258-261），child 成功即用 child 判定；
>   仅 child 失败/缺失才回落父自身检查（272-283）——**child 的格式对接由 child 自己负责**，父不预先替 child 拒绝。
> - **仅有的上混特例 = mono→stereo**：FilterConfiguration.cpp 126-128 注释「模拟无 APO 时 Windows 音频系统自动上混」——
>   EAPO 接管整个 APO 实例后须补做系统默认行为；其他上混/下降混父均不执行（Copy: 显式映射除外）。

| # | 机制 | EAPO | VxAPO 差异 | 带来的设计影响 |
|---|------|------|-----------|----------------|
| D1 | **过渡机制** | 单链 + 配置级过渡：FilterConfiguration 新旧两套 allSamples 平面混合 `doTransition`（157-176）；child 不变 | **父内双链过渡**：outgoing/current 两链交织空间混合 mix_buffers | **child 前置每帧一次，双链共享同一份 child 输出作输入**——child 不在任一链内（独立持有），过渡只切父内链；无需「过渡期 child 双实例」 |
| D2 | **通道语义** | `realChannelCount` 按有无 child 取 in/out；**拒绝下混**（in>out 且 >2ch → S_FALSE + outFormat，277-282）+ **仅 mono→stereo 上混补做**（126-128，补系统默认行为；其他上混/下降混父均不执行） | **等价立场（v8.3 修正表述）**：「收到的输入 == 输出通道数」等价于「**拒绝下混 + 仅 mono→stereo 上混**」（父不转换）；**不引入 realChannelCount 维度切换机制**（EAPO 该分支在协商后为冗余路径）——**实现简化，非更严谨** | ① 双链 temp_buffers/去交织维度稳定（除 mono→stereo 外输入==输出 → 两链输入维度恒等）；② 不支持下混/上混（mono→stereo 除外）与 EAPO 等价；③ 未来需支持通道转换时参考 EAPO 方案 B（维度切换 + mono 上混特例 + Copy: 显式映射），留 P1 后按需 |
| D3 | **child 输出通道数不可知** | child 无报告通道数接口；父按 outFormat 假设 child 已就位输出布局（信任 **APO 链格式传递约定**） | **等价**：同按 outFormat 维度读 child 输出；child 通道布局纯效果由契约声明（安装/配置 child 的责任方保证），**两方均无运行时布局检测** | 无法「约束 child 本身」——布局异常属配置方契约责任（同 EAPO）；VxAPO 仅兜底「收到的输入==输出通道数」校验（父不转换） |
| D4 | **Unlock 失败语义** | childCfg->UnlockForProcess FAILED → **return hr**（父解锁失败，393-401） | **VxAPO 容错**：子解锁失败不阻塞父解锁（object 7.1.10 既定语义） | 差异有意为之：UnlockForProcess 无重试语义，子 APO 可能已部分解锁，父继续自身流程 + 日志（Note 57 降级立场一致） |
| D5 | **IsInputFormatSupported 委托失败** | child 委托失败 → resetChild + 回落自身检查（258-283） | **对齐 + 收窄文案**：child 委托成功用 child 判定；失败回落自身检查（VxAPO 白名单三段式已保证命令/参数错误精准反馈） | 无额外差异 |

> **v8.1 补正说明**：v8.1 初稿「显式约束优于 EAPO 隐式信任」**表述撤销**——经推演，EAPO 靠**拒绝下混**（拒 in>out S_FALSE）+ **mono→stereo 补系统默认** + **协商委托 child** 构成完整通道语义；VxAPO 与之**等价**，唯一区别是不引入 realChannelCount 机制（其 in/out 分支在协商后为冗余/死路径），属**实现简化**。
> **v8.3 修正说明（源码逐行查验）**：①「协商期锁死 in==out」表述失准——EAPO 实际是**拒绝下混 + 仅 mono→stereo 上混**（非全面锁死）；② GetLatency 无 child 返回 0（非「走自身」）；③ child 失败会 resetChild 销毁（非仅回落）；详见 `Equalizer 行为文档.md` S1-S5。

### 18.3 差异化带来的影响（D1-D3 设计后果）

1. **D1 → RT 成本叠加**：child 前置 1 次 + 父双链 2 次 = 每帧 3 次 DSP 执行（10ms 过渡窗口内）；R4（10ms 乱切）在 child 存在时仍成立，但窗口内 CPU 峰值为无 child 的 1.5 倍——**接受**（控制线程切链、RT 仅计算）。
2. **D2 → 维度稳定**：child 不改通道 ⇒ 双链两输入切片维度恒等、temp_buffers/temp_buffer_old/new 共用一份 child 输出输入，无重排逻辑——**这是 VxAPO 双链能叠加 child 的前提**；若未来放宽（方案 B），双链需支持两套通道维度（复杂度显著提升）。
3. **D3 → 校验前置（等价立场）**：LockForProcess「收到的输入 == 输出通道数」校验（父不转换的自身立场，控制线程零 RT 成本）；child 布局契约责任在配置方（同 EAPO）。**VxAPO 不会比 EAPO 多检测任何 child 布局问题，也不会静默错位——等价。**

### 18.4 引用关系

- 本条目的 P0-6（roadmap）：子 APO 委托实现——开放决策 ①②③ 收敛依据即本章（P0-6 Spec-Drafting 定稿时仅需对照确认，不重开设计）。
- 与 `object 7.1.3`（child_apo 字段）、`object 7.1.8`（Initialize 创建降级）、`object 7.1.11`（APOProcess 双链 + child 前置）、`object 7.2`（child.rs 委托方法）配套。
- EAPO 源码参考列：`EqualizerAPO.cpp`（180-215/258-283/341-369/391-424/455-495）、`FilterEngine.h`（129-135）、`FilterConfiguration.cpp`（81-215）。
